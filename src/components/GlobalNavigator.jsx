import { TiThMenuOutline } from "react-icons/ti";

const GlobalNavigator = () => {
    return (
        <nav>
            <button>
                <TiThMenuOutline />
            </button>
        </nav>
    );
};

export default GlobalNavigator;
